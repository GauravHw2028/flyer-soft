import {owner,db,sameOrigin,failure} from '../_shared';
import {workspaceSchema} from '../../validation';
export async function GET(){try{const id=await owner();const row=await db().prepare('SELECT data, revision FROM workspaces WHERE owner = ?').bind(id).first<{data:string;revision:number}>();return Response.json(row?{data:JSON.parse(row.data),revision:row.revision}:{data:null,revision:0},{headers:{'Cache-Control':'no-store'}});}catch(e){return failure(e);}}
export async function PUT(req:Request){try{sameOrigin(req);const id=await owner();const raw=await req.text();if(raw.length>1500000)return Response.json({error:'Workspace limit reached. Export and remove older campaigns.'},{status:413});let body;try{body=JSON.parse(raw)}catch{return Response.json({error:'Invalid request'},{status:400})}const parsed=workspaceSchema.safeParse(body.data);if(!parsed.success||!Number.isSafeInteger(body.revision)||body.revision<0)return Response.json({error:parsed.success?'Invalid revision':parsed.error.issues[0]?.message},{status:400});const images=new Set<string>();for(const p of parsed.data.products)if(p.image.startsWith('/api/assets/'))images.add(p.image.split('/').pop()!);for(const c of parsed.data.campaigns){if(c.brand.logo.startsWith('/api/assets/'))images.add(c.brand.logo.split('/').pop()!);for(const p of c.items)if(p.image.startsWith('/api/assets/'))images.add(p.image.split('/').pop()!)}if(parsed.data.brand.logo.startsWith('/api/assets/'))images.add(parsed.data.brand.logo.split('/').pop()!);if(images.size){const rows=await db().prepare('SELECT id FROM assets WHERE owner = ?').bind(id).all<{id:string}>();const own=new Set(rows.results.map(r=>r.id));if([...images].some(x=>!own.has(x)))return Response.json({error:'An image does not belong to this workspace.'},{status:403});}const data=JSON.stringify(parsed.data),updated=new Date().toISOString();const result=body.revision===0?await db().prepare('INSERT OR IGNORE INTO workspaces (owner, data, revision, updated) VALUES (?, ?, 1, ?)').bind(id,data,updated).run():await db().prepare('UPDATE workspaces SET data = ?, revision = revision + 1, updated = ? WHERE owner = ? AND revision = ?').bind(data,updated,id,body.revision).run();if(!result.meta.changes)return Response.json({error:'This workspace changed in another tab. Download a backup of your edits, then reload.'},{status:409});return Response.json({revision:body.revision+1});}catch(e){return failure(e);}}

